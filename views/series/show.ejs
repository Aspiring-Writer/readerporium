<h2><%= series.name %></h2>

<% if (user.role = "admin") { %>
<div class="row">
  <div class="offset-by-eight two columns">
    <a class="button" href="/series/<%= series.id %>/edit">
      <i class="fa-solid fa-pen fa-lg"></i>
    </a>
  </div>
  <div class="two columns">
    <%- include('../partials/deleteForm', { url: `/series/${series.id}` }) %>
  </div>
</div>
<% } %>
<% if (booksInSeries.length > 0) { %>
<h3>Books in Series</h3>
<% booksInSeries.forEach(book => { %>
<a class="cover" href="/books/<%= book.id %>">
  <img src="<%= book.coverImagePath %>" alt="cover" <% if (book.accessLevel ==
  4) { %>class="blur"<% } %> />
</a>
<% }) %> <% } %>
